apiVersion: apps/v1
kind: Deployment
metadata:
  name: rbtc-mongodb
  namespace: rbtc-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rbtc-mongodb
  template:
    metadata:
      labels:
        app: rbtc-mongodb
    spec:
      containers:
      - name: mongodb
        image: mongo:6.0
        ports:
        - containerPort: 27017
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          value: "admin"
        - name: MONGO_INITDB_ROOT_PASSWORD
          value: "password123"
        volumeMounts:
        - name: mongodb-storage
          mountPath: /data/db
      volumes:
      - name: mongodb-storage
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: rbtc-mongodb-service
  namespace: rbtc-production
spec:
  selector:
    app: rbtc-mongodb
  ports:
  - port: 27017
    targetPort: 27017
  type: ClusterIP